# Пример 1: Приложение в корне репозитория (текущая настройка)
services:
  - type: web
    name: scam-tycoon-empire
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist

# Пример 2: Приложение в подпапке frontend/
# services:
#   - type: web
#     name: scam-tycoon-empire
#     env: static
#     rootDir: frontend
#     buildCommand: npm install && npm run build
#     staticPublishPath: ./dist

# Пример 3: Монорепо с несколькими приложениями
# services:
#   - type: web
#     name: scam-tycoon-web
#     env: static
#     rootDir: apps/web
#     buildCommand: npm install && npm run build
#     staticPublishPath: ./dist
#   
#   - type: web
#     name: scam-tycoon-admin
#     env: static
#     rootDir: apps/admin
#     buildCommand: npm install && npm run build
#     staticPublishPath: ./dist

# Пример 4: Приложение с workspace (pnpm/yarn workspaces)
# services:
#   - type: web
#     name: scam-tycoon-empire
#     env: static
#     rootDir: packages/frontend
#     buildCommand: pnpm install --frozen-lockfile && pnpm run build
#     staticPublishPath: ./dist